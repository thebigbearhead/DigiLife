# === rpi-ops global config ===
# แจ้งเตือน (ถ้าใช้ ntfy self-host ให้ใส่ครบ URL เช่น https://ntfy.example.com/topic)
NTFY_URL="https://ntfy.thebigbearhead.work/RPi-status"

# เส้นทางแบ็กอัป (ตัวอย่าง: โฟลเดอร์งาน -> ฮาร์ดดิสก์ภายนอก)
BACKUP_SRC="/home/raspi"
BACKUP_DST="/mnt/exthddbig/rpi-backup"
BACKUP_EXCLUDES="--exclude=.cache --exclude=node_modules --exclude=.git"

# เว็บที่จะเช็ก (ใช้กับ checkweb@.service, ตัวนี้ไม่ต้องแก้—ใส่ตอน enable)
CHECKWEB_TIMEOUT=10

# Docker compose root (เก็บหลายสแตกอยู่ที่นี่)
DOCKER_ROOT="/home/raspi/docker-old"

# อุณหภูมิสูงสุดที่ถือว่าเสี่ยง (องศา C)
TEMP_WARN=75

# ดิสก์ที่อยากให้สแกน SMART (ปรับตามจริง)
SMART_DISKS="/dev/sda /dev/sdb"

# จำนวนวันเก็บ Journal (ลดท้องผูก)
JOURNAL_MAX_DAYS=7
